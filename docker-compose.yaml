services:
  splitfamily:
    build: .
    command: sh startup.sh
    ports:
      - 6261:8000
    volumes:
      - db-data:/dbs
    environment:
      - CONFIG_NAME=config.conf

networks:
  base:


volumes:
  db-data: